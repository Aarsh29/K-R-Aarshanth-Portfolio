# Release Notes

## v0.0.1

- Added a new avatar image for `SecureAvatar` and imported it cleanly into the app.
- Normalized box spacing and padding across multiple portfolio sections.
- Updated `CyberSidebar`, `TerminalSandbox`, and skills layout for consistent panel styling.
- Confirmed the project builds successfully and pushed the changes to `origin/master`.
